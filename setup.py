from setuptools import setup, find_packages

setup(
    name="scGeneRAI",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "pip==21.2.4",
        "wheel==0.37.0",
    ])
